import SwiftUI

extension Color {
    static let primaryBrand = Color(hex: "#3B82F6")
    static let surfaceDark = Color(hex: "#1E1E2E")
    static let accentSuccess = Color(red: 0.1, green: 0.8, blue: 0.4)
    static let textMuted = Color(hex: "#94A3B8")
}
